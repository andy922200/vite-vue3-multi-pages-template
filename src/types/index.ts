import { DayjsLocales, Locales, mapNumericLocaleToLocale, NumericLocales } from './locales'

enum BuildENV {
  development = 'development',
  production = 'production',
}

export { BuildENV, DayjsLocales, Locales, mapNumericLocaleToLocale, NumericLocales }
