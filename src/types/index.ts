import { DayjsLocales, Locales, NumericLocales, mapNumericLocaleToLocale } from './locales'

enum BuildENV {
  development = 'development',
  production = 'production',
}

export { BuildENV, DayjsLocales, Locales, NumericLocales, mapNumericLocaleToLocale }
